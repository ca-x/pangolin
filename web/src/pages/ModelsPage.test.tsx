import {QueryClient,QueryClientProvider} from '@tanstack/react-query'
import {fireEvent,render,screen,within} from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import {MemoryRouter} from 'react-router'
import {afterEach,describe,expect,it,vi} from 'vitest'
import {ProjectProvider} from '../project'
import '../i18n'
import ModelsPage from './ModelsPage'
const reply=(value:unknown)=>Promise.resolve(new Response(JSON.stringify(value),{status:200,headers:{'Content-Type':'application/json'}}))
const projects=[{id:'p1',name:'P',slug:'p',owner_user_id:'u',is_default:true,enabled:true}]
const routingFetch=(row:Record<string,unknown>)=>vi.fn((input:RequestInfo|URL,init?:RequestInit)=>{const path=String(input);if(path.endsWith('/projects'))return reply(projects);if(path.includes('/permissions'))return reply(['*']);if(path.includes('/api-keys'))return reply([]);if(init?.method==='POST')return reply({id:row.id||'a1'});if(path.includes('/associations'))return reply({data:[row],total:1});return reply({data:[],total:0})})
const renderPage=(fetchMock:ReturnType<typeof vi.fn>)=>{vi.stubGlobal('fetch',fetchMock);const client=new QueryClient({defaultOptions:{queries:{retry:false}}});return render(<QueryClientProvider client={client}><MemoryRouter><ProjectProvider><ModelsPage/></ProjectProvider></MemoryRouter></QueryClientProvider>)}
afterEach(()=>vi.unstubAllGlobals())

describe('routing association editor',()=>{
  it('round-trips unrestricted selectors and the exact nested condition document',async()=>{
    const conditions={version:1,all:[{field:'/stream',op:'eq',value:true},{any:[{field:'/has_image',op:'eq',value:true},{field:'/request_format',op:'eq',value:'openai_responses'}]}]}
    const fetchMock=routingFetch({id:'a1',model_id:null,model_name:null,provider_id:null,provider_name:null,match_type:'exact',pattern:'global',conditions,exclusions:{version:1},priority:1,weight:1,enabled:1})
    renderPage(fetchMock);await userEvent.click(await screen.findByRole('tab',{name:'Model routing'}));await userEvent.click((await screen.findAllByRole('button',{name:'Edit'}))[0]);expect(screen.getAllByDisplayValue('Unrestricted').length).toBeGreaterThanOrEqual(2);expect(screen.getByText('All conditions')).toBeInTheDocument();expect(screen.getByText('Any condition')).toBeInTheDocument();await userEvent.click(screen.getByRole('button',{name:'Save'}));await vi.waitFor(()=>{const call=fetchMock.mock.calls.find(([,init])=>init?.method==='POST');const body=JSON.parse(String(call?.[1]?.body));expect(body.id).toBe('a1');expect(body.model_id).toBeNull();expect(body.provider_id).toBeNull();expect(body.conditions).toEqual(conditions)})
  },10000)
  it('keeps raw JSON as an advanced escape hatch with inline validation',async()=>{
    const fetchMock=routingFetch({id:'a1',model_id:null,provider_id:null,match_type:'exact',pattern:'global',conditions:{version:1},exclusions:{version:1},priority:1,weight:1,enabled:1})
    renderPage(fetchMock);await userEvent.click(await screen.findByRole('tab',{name:'Model routing'}));await userEvent.click((await screen.findAllByRole('button',{name:'Edit'}))[0]);await userEvent.click(screen.getByRole('button',{name:'Raw JSON'}));const raw=screen.getByRole('textbox',{name:'Conditions JSON'});fireEvent.change(raw,{target:{value:'{bad'}});expect(await screen.findByRole('alert')).toHaveTextContent('Enter valid JSON.');expect(screen.getByRole('button',{name:'Save'})).toBeDisabled();fireEvent.change(raw,{target:{value:'{"version":1,"field":"/body/metadata/tier","op":"eq","value":"gold"}'}});await userEvent.click(screen.getByRole('button',{name:'Save'}));await vi.waitFor(()=>{const call=fetchMock.mock.calls.find(([,init])=>init?.method==='POST');const body=JSON.parse(String(call?.[1]?.body));expect(body.conditions).toEqual({version:1,field:'/body/metadata/tier',op:'eq',value:'gold'})})
  })
  it('expresses channel-tag regex matching and every channel exclusion form',async()=>{
    const fetchMock=routingFetch({id:'a1',model_id:null,provider_id:null,match_type:'exact',pattern:'global',conditions:{version:1},exclusions:{version:1},priority:1,weight:1,enabled:1})
    renderPage(fetchMock);await userEvent.click(await screen.findByRole('tab',{name:'Model routing'}));await userEvent.click((await screen.findAllByRole('button',{name:'Edit'}))[0]);await userEvent.click(screen.getByRole('combobox',{name:'Match type'}));await userEvent.click(await screen.findByRole('option',{name:'Channel tags (regex)'}));const pattern=screen.getByRole('textbox',{name:'Pattern'});await userEvent.clear(pattern);await userEvent.type(pattern,'^region-(eu|us)$');await userEvent.type(screen.getByRole('textbox',{name:'Channel name patterns'}),'^legacy-,^test-');await userEvent.type(screen.getByRole('textbox',{name:'Excluded channel IDs'}),'provider-a, provider-b');await userEvent.type(screen.getByRole('textbox',{name:'Excluded channel tags'}),'private, deprecated');await userEvent.click(screen.getByRole('button',{name:'Save'}));await vi.waitFor(()=>{const call=fetchMock.mock.calls.find(([,init])=>init?.method==='POST');const body=JSON.parse(String(call?.[1]?.body));expect(body.match_type).toBe('channel_tags_regex');expect(body.exclusions).toEqual({version:1,channel_name_patterns:['^legacy-','^test-'],channel_ids:['provider-a','provider-b'],channel_tags:['private','deprecated']})})
  })
})

describe('model catalog card columns',()=>{it('renders typed card values, bundled icons, fallbacks, and unmeasured facts',async()=>{const models=[{id:'known',provider_id:'channel-1',provider_name:'Primary',public_name:'gpt-public',upstream_name:'gpt-upstream',capabilities:['chat','responses'],enabled:1,catalog_developer:'OpenAI',catalog_model_type:'chat',catalog_logo_key:'lobehub:OpenAI',catalog_context_limit_tokens:128000,catalog_output_limit_tokens:16384,catalog_input_cost:2.5,catalog_output_cost:10,catalog_cost_currency:'USD',catalog_cost_unit:'per_million_tokens'},{id:'fallback',provider_id:'channel-1',provider_name:'Primary',public_name:'acme-public',upstream_name:'acme-upstream',capabilities:['chat'],enabled:1,catalog_developer:'Acme Labs',catalog_model_type:'chat',catalog_logo_key:'catalog:missing',catalog_context_limit_tokens:null,catalog_output_limit_tokens:null,catalog_input_cost:null,catalog_output_cost:null,catalog_cost_currency:null,catalog_cost_unit:null},{id:'manual',provider_id:'channel-1',provider_name:'Primary',public_name:'manual-public',upstream_name:'manual-upstream',capabilities:['chat'],enabled:1,catalog_developer:null,catalog_model_type:null,catalog_logo_key:null,catalog_context_limit_tokens:null,catalog_output_limit_tokens:null,catalog_input_cost:null,catalog_output_cost:null,catalog_cost_currency:null,catalog_cost_unit:null}];const fetchMock=vi.fn((input:RequestInfo|URL)=>{const path=String(input);if(path.endsWith('/projects'))return reply([{id:'p1',name:'P',slug:'p',owner_user_id:'u',is_default:true,enabled:true}]);if(path.includes('/permissions'))return reply(['*']);if(path.includes('/operations/models'))return reply({data:models,total:models.length});if(path.includes('/operations/channels'))return reply({data:[{id:'channel-1',name:'Primary'}],total:1});if(path.includes('/catalog/models'))return reply({data:[],total:0});return reply({data:[],total:0})});vi.stubGlobal('fetch',fetchMock);const client=new QueryClient({defaultOptions:{queries:{retry:false}}});const view=render(<QueryClientProvider client={client}><MemoryRouter><ProjectProvider><ModelsPage/></ProjectProvider></MemoryRouter></QueryClientProvider>);await screen.findAllByText('gpt-public');const table=view.container.querySelector('.desktop-resource-table') as HTMLElement;expect(within(table).getByLabelText('OpenAI')).toBeInTheDocument();expect(within(table).getByLabelText('Acme Labs')).toHaveTextContent('AL');expect(within(table).getByText('128,000 tokens')).toBeInTheDocument();expect(within(table).getByText('16,384 tokens')).toBeInTheDocument();expect(within(table).getByText('2.5 USD / million tokens')).toBeInTheDocument();expect(within(table).getByText('10 USD / million tokens')).toBeInTheDocument();expect(within(within(table).getByRole('row',{name:/manual-public/})).getAllByText('—').length).toBeGreaterThanOrEqual(4);const cards=view.container.querySelector('.mobile-resource-list') as HTMLElement;expect(within(cards).getByText('gpt-upstream')).toBeInTheDocument();expect(within(cards).getAllByText('OpenAI').length).toBeGreaterThan(0);expect(within(cards).getByText('128,000 tokens')).toBeInTheDocument();expect(within(cards).getByText('2.5 USD / million tokens')).toBeInTheDocument()})})
